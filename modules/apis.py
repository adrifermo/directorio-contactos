import random


class Helpers:
    @staticmethod
    def validate_data(contact):
        return random.choice([True, False])

    @staticmethod
    def validate_background(contact):
        return random.choice([True, False])

